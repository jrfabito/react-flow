// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
import React from 'react';
import clsx from 'clsx';
import { getBaseProps } from '../internal/base-component';
import InternalStructuredItem from '../internal/components/structured-item';
import { useVisualRefresh } from '../internal/hooks/use-visual-mode';
import { processAttributes } from '../internal/utils/with-native-attributes';
import { getContentStyles, getFooterStyles, getHeaderStyles, getRootStyles } from './style';
import styles from './styles.css.js';
import testStyles from './test-classes/styles.css.js';
export default function InternalItemCard({ actions, highlighted, children, className, header, description, footer, icon, style, metadataAttributes, nativeAttributes, onClick, disableHeaderPaddings, disableContentPaddings, disableFooterPaddings, fullHeight, variant = 'default', __internalRootRef, ...restProps }) {
    const baseProps = getBaseProps(restProps);
    const isRefresh = useVisualRefresh();
    const headerRowEmpty = !header && !description && !icon && !actions;
    const rootAttributes = processAttributes({
        className: clsx(className, styles.root, highlighted && styles.highlighted, fullHeight && styles['full-height'], isRefresh && styles.refresh, styles[`variant-${variant}`]),
        ...metadataAttributes,
        onClick,
        style: getRootStyles(style),
    }, nativeAttributes, 'Card');
    return (React.createElement("div", { ref: __internalRootRef, ...baseProps, ...rootAttributes },
        React.createElement("div", { className: styles['inner-card'] },
            !headerRowEmpty && (React.createElement("div", { className: clsx(styles.header, disableHeaderPaddings && styles['no-padding'], !!actions && styles['with-actions']), style: getHeaderStyles(style) },
                React.createElement(InternalStructuredItem, { content: header && React.createElement("div", { className: clsx(styles['header-inner'], testStyles.header) }, header), secondaryContent: description && React.createElement("div", { className: clsx(styles.description, testStyles.description) }, description), icon: icon && React.createElement("div", { className: testStyles.icon }, icon), actions: actions, disablePaddings: disableHeaderPaddings, wrapActions: false }))),
            children && (React.createElement("div", { className: clsx(styles.body, testStyles.body, disableContentPaddings && styles['no-padding']), style: getContentStyles(style) }, children)),
            footer && (React.createElement("div", { className: clsx(styles.footer, testStyles.footer, disableFooterPaddings && styles['no-padding']), style: getFooterStyles(style) }, footer)))));
}
//# sourceMappingURL=internal.js.map