import { InternalBaseComponentProps } from '../internal/hooks/use-base-component/index.js';
import { CalendarProps } from './interfaces.js';
type InternalCalendarProps = CalendarProps & InternalBaseComponentProps & {
  referrerId?: string;
};
export default function Calendar({
  value,
  locale,
  startOfWeek,
  isDateEnabled,
  dateDisabledReason,
  ariaLabel,
  ariaLabelledby,
  ariaDescribedby,
  onChange,
  __internalRootRef,
  i18nStrings,
  granularity,
  referrerId,
  previousMonthAriaLabel,
  nextMonthAriaLabel,
  todayAriaLabel,
  ...rest
}: InternalCalendarProps): JSX.Element;
export {};