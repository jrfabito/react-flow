import React from 'react';
import { AppLayoutProps } from '../../../app-layout/interfaces';
import { BreadcrumbGroupProps } from '../../../breadcrumb-group/interfaces';
interface ToolbarContainerProps {
  children: React.ReactNode;
  hasAiDrawer?: boolean;
}
export declare function ToolbarContainer({
  children,
  hasAiDrawer
}: ToolbarContainerProps): JSX.Element;
interface ToolbarBreadcrumbsSectionProps {
  ownBreadcrumbs: React.ReactNode;
  discoveredBreadcrumbs?: BreadcrumbGroupProps | null;
  includeTestUtils?: boolean;
}
export declare function ToolbarBreadcrumbsSection({
  ownBreadcrumbs,
  discoveredBreadcrumbs,
  includeTestUtils
}: ToolbarBreadcrumbsSectionProps): JSX.Element;
interface ToolbarSkeletonStructureProps {
  ariaLabels?: AppLayoutProps.Labels;
  expandedDrawerId?: string | null;
  hasNavigation?: boolean;
  navigationOpen?: boolean;
  ownBreadcrumbs: React.ReactNode;
  discoveredBreadcrumbs?: BreadcrumbGroupProps | null;
}
export declare const ToolbarSkeletonStructure: React.ForwardRefExoticComponent<ToolbarSkeletonStructureProps & React.RefAttributes<HTMLElement>>;
export {};