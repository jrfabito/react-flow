import { t as padLeftZeros } from "./pad-left-zeros-E5m_97eE.js";
//#region node_modules/@cloudscape-design/components/internal/utils/date-time/format-date.js
/**
* Transforms Date's object date part to a string.
*
* We cannot use Date.toISOString because it produces GMT time where the date can be different than local.
*/
function formatDate(value, granularity = "day") {
	const year = value.getFullYear();
	const month = padLeftZeros(`${value.getMonth() + 1}`, 2);
	if (granularity === "month") return `${year}-${month}`;
	return `${year}-${month}-${padLeftZeros(`${value.getDate()}`, 2)}`;
}
//#endregion
export { formatDate as t };

//# sourceMappingURL=format-date-BfESnizI.js.map