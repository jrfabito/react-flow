import { n as __toESM } from "./chunk-FDOR9p9I.js";
import { t as require_react } from "./react.js";
import { d as clsx, x as warnOnce } from "./base-component-DsNr9y5n.js";
//#region node_modules/@cloudscape-design/components/internal/utils/with-native-attributes.js
var import_react = /* @__PURE__ */ __toESM(require_react());
function processAttributes(rest, nativeAttributes, componentName, skipWarnings) {
	return Object.entries(nativeAttributes || {}).reduce((acc, [key, value]) => {
		if (key === "className") acc[key] = clsx(rest.className, value);
		else if (key === "style") acc[key] = {
			...rest.style,
			...value
		};
		else if (key.match(/^on[A-Z]/) && typeof value === "function" && key in rest) acc[key] = (event) => {
			value(event);
			if (!event.defaultPrevented) rest[key](event);
		};
		else {
			if (key in rest && (!skipWarnings || skipWarnings !== true && !skipWarnings.includes(key))) warnOnce(componentName, `Overriding native attribute [${key}] which has a Cloudscape-provided value`);
			acc[key] = value;
		}
		return acc;
	}, { ...rest });
}
var with_native_attributes_default = import_react.forwardRef(({ tag, nativeAttributes, children, skipWarnings, componentName, ...rest }, ref) => {
	const Tag = tag;
	const processedAttributes = processAttributes(rest, nativeAttributes, componentName, skipWarnings);
	return import_react.createElement(Tag, {
		...processedAttributes,
		ref
	}, children);
});
//#endregion
export { with_native_attributes_default as n, processAttributes as t };

//# sourceMappingURL=with-native-attributes-BTfK0ncD.js.map