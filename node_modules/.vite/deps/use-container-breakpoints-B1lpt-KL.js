import { t as useContainerQuery } from "./use-container-query-D8eE-t6m.js";
import { n as getMatchingBreakpoint } from "./breakpoints-1ZVW1PYe.js";
//#region node_modules/@cloudscape-design/components/internal/hooks/container-queries/use-container-breakpoints.js
/**
* Re-renders the component when the breakpoint for the component changes. Scopes the re-renders to the specific
* breakpoints you want to break at. "default" is always included as a fallback, so ["xs"] would trigger for
* "default" and "xs".
*
* @param triggers The relevant breakpoints to trigger for.
*/
function useContainerBreakpoints(triggers) {
	return useContainerQuery((rect) => getMatchingBreakpoint(rect.contentBoxWidth, triggers), [triggers === null || triggers === void 0 ? void 0 : triggers.join()]);
}
//#endregion
export { useContainerBreakpoints as t };

//# sourceMappingURL=use-container-breakpoints-B1lpt-KL.js.map