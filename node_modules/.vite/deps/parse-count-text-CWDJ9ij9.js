//#region node_modules/@cloudscape-design/components/internal/analytics/utils/parse-count-text.js
/**
* Extracts the count value from table header/filter text.
*
* Parses various counter string formats and extracts the relevant numeric value:
* - "Items (100)" - Extracts 100 (first number found)
* - "1/100" - Extracts 100 (denominator of fraction, representing total count)
* - "100+" - Extracts 100 (first number found)
*/
var parseCountValue = (countText) => {
	if (!countText || typeof countText !== "string") return;
	const match = (countText.includes("/") ? countText.split("/")[1] : countText).match(/\d+/);
	return match ? parseInt(match[0], 10) : void 0;
};
//#endregion
export { parseCountValue as t };

//# sourceMappingURL=parse-count-text-CWDJ9ij9.js.map