import { n as __toESM } from "./chunk-FDOR9p9I.js";
import { t as require_react } from "./react.js";
import { t as require_react_dom } from "./react-dom.js";
import "/Users/jrfabito/Documents/Job Search/react-flow/node_modules/@cloudscape-design/components/internal/base-component/styles.scoped.css";
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/base-component/component-metadata.js
var import_react = /* @__PURE__ */ __toESM(require_react(), 1);
var COMPONENT_METADATA_KEY = "__awsuiMetadata__";
function useComponentMetadata(componentName, packageMetadata, analyticsMetadata) {
	const elementRef = (0, import_react.useRef)(null);
	(0, import_react.useEffect)(() => {
		if (elementRef.current) {
			const pkgMetadata = typeof packageMetadata === "string" ? { version: packageMetadata } : packageMetadata;
			const node = elementRef.current;
			const metadata = {
				...pkgMetadata,
				name: componentName
			};
			if (analyticsMetadata && Object.keys(analyticsMetadata).length > 0) metadata.analytics = analyticsMetadata;
			Object.freeze(metadata);
			Object.defineProperty(node, COMPONENT_METADATA_KEY, {
				value: metadata,
				writable: false,
				configurable: true
			});
		}
	});
	return elementRef;
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/base-component/metrics/log-clients.js
var AWSUI_EVENT = "awsui";
function validateLength(value, maxLength) {
	return !value || value.length <= maxLength;
}
/**
* Console Platform's client logging JS API client.
*/
var CLogClient = class {
	/**
	* Sends metric but only if Console Platform client logging JS API is present in the page.
	*/
	sendMetric(metricName, value, detail) {
		if (!metricName || !/^[a-zA-Z0-9_-]+$/.test(metricName)) {
			console.error(`Invalid metric name: ${metricName}`);
			return;
		}
		if (!validateLength(metricName, 1e3)) {
			console.error(`Metric name ${metricName} is too long`);
			return;
		}
		if (!validateLength(detail, 4e3)) {
			console.error(`Detail for metric ${metricName} is too long: ${detail}`);
			return;
		}
		if (new PanoramaClient().sendMetric({
			eventContext: metricName,
			eventDetail: detail,
			eventValue: `${value}`,
			timestamp: Date.now()
		})) return;
		const AWSC = this.findAWSC(window);
		if (typeof AWSC === "object" && typeof AWSC.Clog === "object" && typeof AWSC.Clog.log === "function") AWSC.Clog.log(metricName, value, detail);
	}
	findAWSC(currentWindow) {
		try {
			if (typeof (currentWindow === null || currentWindow === void 0 ? void 0 : currentWindow.AWSC) === "object") return currentWindow === null || currentWindow === void 0 ? void 0 : currentWindow.AWSC;
			if (!currentWindow || currentWindow.parent === currentWindow) return;
			return this.findAWSC(currentWindow.parent);
		} catch {
			return;
		}
	}
};
/**
* Console Platform's client v2 logging JS API client.
*/
var PanoramaClient = class {
	/**
	* Sends metric but only if Console Platform client v2 logging JS API is present in the page.
	*/
	sendMetric(metric) {
		const panorama = this.findPanorama(window);
		if (!panorama) return false;
		const payload = {
			eventType: AWSUI_EVENT,
			timestamp: Date.now(),
			...metric,
			eventDetail: typeof metric.eventDetail === "object" ? JSON.stringify(metric.eventDetail) : metric.eventDetail,
			eventValue: typeof metric.eventValue === "object" ? JSON.stringify(metric.eventValue) : metric.eventValue
		};
		if (!validateLength(payload.eventDetail, 4e3)) {
			this.onMetricError(`Event detail for metric is too long: ${payload.eventDetail}`);
			return true;
		}
		if (!validateLength(payload.eventValue, 4e3)) {
			this.onMetricError(`Event value for metric is too long: ${payload.eventValue}`);
			return true;
		}
		if (!validateLength(payload.eventContext, 4e3)) {
			this.onMetricError(`Event context for metric is too long: ${payload.eventContext}`);
			return true;
		}
		panorama("trackCustomEvent", payload);
		return true;
	}
	onMetricError(message) {
		console.error(message);
		const panorama = this.findPanorama(window);
		if (panorama) panorama("trackCustomEvent", {
			eventType: AWSUI_EVENT,
			eventContext: "awsui-metric-error",
			eventDetail: message.slice(0, 4e3),
			timestamp: Date.now()
		});
	}
	findPanorama(currentWindow) {
		try {
			if (typeof (currentWindow === null || currentWindow === void 0 ? void 0 : currentWindow.panorama) === "function") return currentWindow === null || currentWindow === void 0 ? void 0 : currentWindow.panorama;
			const symbolProperty = currentWindow === null || currentWindow === void 0 ? void 0 : currentWindow[Symbol.for("panorama")];
			if (typeof symbolProperty === "function") return symbolProperty;
			if (!currentWindow || currentWindow.parent === currentWindow) return;
			return this.findPanorama(currentWindow.parent);
		} catch {
			return;
		}
	}
};
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/base-component/metrics/formatters.js
function buildMetricDetail(detail, context) {
	return jsonStringify({
		o: typeof AWSUI_METRIC_ORIGIN !== "undefined" ? AWSUI_METRIC_ORIGIN : "main",
		t: context.theme,
		f: "react",
		v: context.packageVersion.replace(/\s/g, ""),
		...detail
	});
}
function buildComponentMetricDetail({ componentName, action, configuration, packageSource }, context) {
	return buildMetricDetail({
		a: action,
		s: componentName,
		p: packageSource,
		c: configuration
	}, context);
}
function jsonStringify(detailObject) {
	return JSON.stringify(detailObject, detailSerializer);
}
function detailSerializer(key, value) {
	if (typeof value === "number" && !Number.isFinite(value)) return `${value}`;
	return value;
}
function getMajorVersion(versionString) {
	const majorVersionMatch = versionString.match(/^(\d+\.\d+)/);
	return majorVersionMatch ? majorVersionMatch[1].replace(".", "") : "";
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/base-component/metrics/metrics.js
var oneTimeMetrics = /* @__PURE__ */ new Set();
var Metrics = class {
	constructor(...args) {
		this.clog = new CLogClient();
		this.panorama = new PanoramaClient();
		if (args.length === 1) this.context = args[0];
		else {
			const [packageSource, packageVersion] = args;
			this.context = {
				packageSource,
				packageVersion,
				theme: "unknown"
			};
		}
	}
	sendComponentMetric(metric) {
		this.sendMetricOnce(`awsui_${metric.componentName}_${this.context.theme.charAt(0)}${getMajorVersion(this.context.packageVersion)}`, 1, buildComponentMetricDetail(metric, this.context));
	}
	sendMetricOnce(metricName, value, detail) {
		const key = [metricName + value + detail].join("|");
		if (!oneTimeMetrics.has(key)) {
			this.clog.sendMetric(metricName, value, detail);
			oneTimeMetrics.add(key);
		}
	}
	/**
	* Calls Console Platform's client v2 logging JS API with provided metric name and detail.
	* Does nothing if Console Platform client logging JS is not present in page.
	*/
	sendPanoramaMetric(metric) {
		this.panorama.sendMetric(metric);
	}
	sendOpsMetricObject(metricName, detail) {
		this.sendMetricOnce(metricName, 1, buildMetricDetail(detail, this.context));
	}
	sendOpsMetricValue(metricName, value) {
		this.sendMetricOnce(metricName, value);
	}
	logComponentsLoaded() {
		this.sendComponentMetric({
			componentName: this.context.packageSource,
			action: "loaded"
		});
	}
	logComponentUsed(componentName, configuration) {
		this.sendComponentMetric({
			action: "used",
			componentName,
			configuration,
			packageSource: this.context.packageSource
		});
	}
};
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/base-component/component-metrics.js
function useComponentMetrics(componentName, settings, configuration = { props: {} }) {
	(0, import_react.useEffect)(() => {
		const metrics = new Metrics(settings);
		if (typeof window !== "undefined") {
			metrics.sendOpsMetricValue("awsui-viewport-width", window.innerWidth || 0);
			metrics.sendOpsMetricValue("awsui-viewport-height", window.innerHeight || 0);
		}
		metrics.logComponentsLoaded();
		metrics.logComponentUsed(componentName.toLowerCase(), configuration);
	}, []);
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/base-component/init-awsui-versions.js
function initAwsUiVersions(source, packageVersion) {
	if (typeof window !== "undefined") {
		if (!window.awsuiVersions) window.awsuiVersions = {};
		if (!window.awsuiVersions[source]) window.awsuiVersions[source] = [];
		window.awsuiVersions[source].push(packageVersion);
	}
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/stable-callback/index.js
var import_react_dom = /* @__PURE__ */ __toESM(require_react_dom(), 1);
/**
* A callback that stays stable between renders even as the dependencies change.
* Not a recommended React pattern, so it should be used sparingly and only if
* the callback is used asynchronously (i.e. not used during rendering) and causing
* clear performance issues.
*
* @remarks
*
* The implementation ensures the callback cannot be called synchronously. All synchronous calls
* (during rendering) are ignored.
*
* @example
* Use stable onMouseMove handler
* ```
* function Demo({ args }) {
*   const stableOnMouseMove = useStableCallback((event) => makeAction(event, args))
*   return <Container onMouseMove={stableOnMouseMove} />
* }
* ```
*
* @see https://reactjs.org/docs/hooks-faq.html#how-to-read-an-often-changing-value-from-usecallback
*
* @typeParam Callback The callback to be made stable
* @returns Stable callback
*/
function useStableCallback(fn) {
	const ref = (0, import_react.useRef)();
	(0, import_react.useEffect)(() => {
		ref.current = fn;
	});
	return (0, import_react.useCallback)((...args) => {
		var _a;
		return (_a = ref.current) === null || _a === void 0 ? void 0 : _a.call(ref, ...args);
	}, []);
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/singleton-handler/index.js
function createSingletonHandler(factory) {
	const listeners = /* @__PURE__ */ new Set();
	const callback = (value) => {
		(0, import_react_dom.unstable_batchedUpdates)(() => {
			for (const listener of listeners) listener(value);
		});
	};
	let cleanup;
	return function useSingleton(listener) {
		(0, import_react.useEffect)(() => {
			if (listeners.size === 0) cleanup = factory(callback);
			listeners.add(listener);
			return () => {
				listeners.delete(listener);
				if (listeners.size === 0) {
					cleanup();
					cleanup = void 0;
				}
			};
		}, []);
	};
}
function createSingletonState({ factory, initialState }) {
	const useSingleton = createSingletonHandler(factory);
	return function useSingletonState() {
		const [state, setState] = (0, import_react.useState)(initialState);
		useSingleton(setState);
		return state;
	};
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/dom/element-types.js
function isNode(target) {
	return target instanceof Node || target !== null && typeof target === "object" && "nodeType" in target && typeof target.nodeType === "number" && "nodeName" in target && typeof target.nodeName === "string" && "parentNode" in target && typeof target.parentNode === "object";
}
function isHTMLElement(target) {
	return target instanceof HTMLElement || isNode(target) && target.nodeType === Node.ELEMENT_NODE && "style" in target && typeof target.style === "object" && typeof target.ownerDocument === "object" && !isSVGElement(target);
}
function isSVGElement(target) {
	return target instanceof SVGElement || isNode(target) && target.nodeType === Node.ELEMENT_NODE && "ownerSVGElement" in target && typeof target.ownerSVGElement === "object";
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/dom/find-up-until.js
/**
* Checks if the current element or any of its parent is matched with `test` function.
*
* @example
* Check if there is an ancestor with a CSS class matching regex
* ```
* const matchContext = (element) => element.className.match(/my-context-([\w-]+)/)
* const contextElement = findUpUntil(currentElement, matchContext)
* ```
*
* @param from Element to search from
* @param test Returns `true` if the given element satisfies the search criteria
* @returns First matched element or `null`
*/
function findUpUntil(from, test) {
	let current = from;
	while (current && !test(current)) {
		current = current.parentElement;
		while (current && !isHTMLElement(current)) current = current.parentElement;
	}
	return current;
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/logging.js
var messageCache = /* @__PURE__ */ new Set();
function warnOnce(component, message) {
	{
		const warning = `[AwsUi] [${component}] ${message}`;
		if (!messageCache.has(warning)) {
			messageCache.add(warning);
			console.warn(warning);
		}
	}
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/global-flags/index.js
var awsuiVisualRefreshFlag = Symbol.for("awsui-visual-refresh-flag");
var awsuiGlobalFlagsSymbol = Symbol.for("awsui-global-flags");
var getTopWindow = () => {
	return window.top;
};
function getGlobal() {
	return typeof window !== "undefined" ? window : globalThis;
}
function readFlag(holder, flagName) {
	var _a;
	return (_a = holder === null || holder === void 0 ? void 0 : holder[awsuiGlobalFlagsSymbol]) === null || _a === void 0 ? void 0 : _a[flagName];
}
var getGlobalFlag = (flagName) => {
	try {
		const ownFlag = readFlag(getGlobal(), flagName);
		if (ownFlag !== void 0) return ownFlag;
		return readFlag(getTopWindow(), flagName);
	} catch {
		return;
	}
};
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/utils/safe-match-media.js
function safeMatchMedia(element, query) {
	var _a, _b, _c, _d;
	try {
		const targetWindow = (_b = (_a = element.ownerDocument) === null || _a === void 0 ? void 0 : _a.defaultView) !== null && _b !== void 0 ? _b : window;
		return (_d = (_c = targetWindow.matchMedia) === null || _c === void 0 ? void 0 : _c.call(targetWindow, query).matches) !== null && _d !== void 0 ? _d : false;
	} catch (error) {
		console.warn(error);
		return false;
	}
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/visual-mode/index.js
function isMotionDisabled(element) {
	return !!findUpUntil(element, (node) => node.classList.contains("awsui-motion-disabled")) || safeMatchMedia(element, "(prefers-reduced-motion: reduce)");
}
function useModeDetector(elementRef, detector, initialValue) {
	const [value, setValue] = (0, import_react.useState)(initialValue);
	useMutationObserver(elementRef, (node) => {
		const newValue = detector(node);
		/**
		* React has a behavior that triggers a re-render even if the same value is provided in the setState, while it does not
		* commit any changes to the DOM (commit phase) the function rerenders. This causes a false react act warnings in testing
		* and any component using the Transition component which in return uses this hook will possibly have false react warnings.
		*
		* To fix this, we manually stop setting the state ourselves if we see the same value.
		* References:  https://www.reddit.com/r/reactjs/comments/1ej505e/why_does_it_rerender_even_when_state_is_same/#:~:text=If%20the%20new%20value%20you,shouldn't%20affect%20your%20code
		*/
		if (newValue !== value) setValue(newValue);
	});
	return value;
}
function detectCurrentMode(node) {
	return findUpUntil(node, (node) => node.classList.contains("awsui-polaris-dark-mode") || node.classList.contains("awsui-dark-mode")) ? "dark" : "light";
}
function detectDensityMode(node) {
	return findUpUntil(node, (node) => node.classList.contains("awsui-polaris-compact-mode") || node.classList.contains("awsui-compact-mode")) ? "compact" : "comfortable";
}
function useCurrentMode(elementRef) {
	return useModeDetector(elementRef, detectCurrentMode, "light");
}
function useDensityMode(elementRef) {
	return useModeDetector(elementRef, detectDensityMode, "comfortable");
}
function useReducedMotion(elementRef) {
	return useModeDetector(elementRef, isMotionDisabled, false);
}
var useMutationSingleton = createSingletonHandler((handler) => {
	const observer = new MutationObserver(() => handler());
	observer.observe(document.body, {
		attributes: true,
		subtree: true
	});
	return () => observer.disconnect();
});
function useMutationObserver(elementRef, onChange) {
	const handler = useStableCallback(() => {
		if (elementRef.current) onChange(elementRef.current);
	});
	useMutationSingleton(handler);
	(0, import_react.useEffect)(() => {
		handler();
	}, [handler]);
}
var visualRefreshState = void 0;
function detectVisualRefreshClassName() {
	return typeof document !== "undefined" && !!document.querySelector(".awsui-visual-refresh, .awsui-one-theme");
}
function detectVisualRefreshFlag() {
	var _a, _b;
	const global = getGlobal();
	return (_b = (_a = global === null || global === void 0 ? void 0 : global[awsuiVisualRefreshFlag]) === null || _a === void 0 ? void 0 : _a.call(global)) !== null && _b !== void 0 ? _b : !!getGlobalFlag("oneTheme");
}
function useRuntimeVisualRefresh() {
	if (visualRefreshState === void 0) {
		visualRefreshState = detectVisualRefreshClassName();
		if (!visualRefreshState) {
			if (detectVisualRefreshFlag()) {
				visualRefreshState = true;
				if (typeof document !== "undefined") document.body.classList.add("awsui-visual-refresh");
			}
		}
	}
	if ((detectVisualRefreshClassName() || detectVisualRefreshFlag()) !== visualRefreshState) warnOnce("Visual Refresh", "Dynamic visual refresh change detected. This is not supported. Make sure `awsui-visual-refresh` is attached to the `<body>` element before initial React render");
	return visualRefreshState;
}
var Theme;
(function(Theme) {
	Theme["VisualRefresh"] = "visual-refresh";
	Theme["OneTheme"] = "one-theme";
})(Theme || (Theme = {}));
var THEMES = {
	[Theme.VisualRefresh]: {
		className: "awsui-visual-refresh",
		isFlagActive: () => {
			var _a, _b;
			return !!((_b = (_a = getGlobal()) === null || _a === void 0 ? void 0 : _a[awsuiVisualRefreshFlag]) === null || _b === void 0 ? void 0 : _b.call(_a));
		}
	},
	[Theme.OneTheme]: {
		className: "awsui-one-theme",
		isFlagActive: () => !!getGlobalFlag("oneTheme")
	}
};
function isThemeActive(theme) {
	const config = THEMES[theme];
	if (typeof document !== "undefined" && document.querySelector(`.${config.className}`)) return true;
	return config.isFlagActive();
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/keycode.js
var KeyCode;
(function(KeyCode) {
	KeyCode[KeyCode["pageUp"] = 33] = "pageUp";
	KeyCode[KeyCode["pageDown"] = 34] = "pageDown";
	KeyCode[KeyCode["end"] = 35] = "end";
	KeyCode[KeyCode["home"] = 36] = "home";
	KeyCode[KeyCode["backspace"] = 8] = "backspace";
	KeyCode[KeyCode["space"] = 32] = "space";
	KeyCode[KeyCode["down"] = 40] = "down";
	KeyCode[KeyCode["left"] = 37] = "left";
	KeyCode[KeyCode["right"] = 39] = "right";
	KeyCode[KeyCode["up"] = 38] = "up";
	KeyCode[KeyCode["escape"] = 27] = "escape";
	KeyCode[KeyCode["enter"] = 13] = "enter";
	KeyCode[KeyCode["tab"] = 9] = "tab";
	KeyCode[KeyCode["shift"] = 16] = "shift";
	KeyCode[KeyCode["control"] = 17] = "control";
	KeyCode[KeyCode["alt"] = 18] = "alt";
	KeyCode[KeyCode["meta"] = 91] = "meta";
})(KeyCode || (KeyCode = {}));
function isModifierKey(event) {
	return [
		KeyCode.shift,
		KeyCode.alt,
		KeyCode.control,
		KeyCode.meta
	].includes(event.keyCode);
}
//#endregion
//#region node_modules/@cloudscape-design/component-toolkit/mjs/internal/focus-visible/index.js
var frames = /* @__PURE__ */ new Map();
function setIsKeyboard(active) {
	if (active) for (const currentDocument of frames.keys()) currentDocument.body.setAttribute("data-awsui-focus-visible", "true");
	else for (const currentDocument of frames.keys()) currentDocument.body.removeAttribute("data-awsui-focus-visible");
}
function handleMousedown() {
	setIsKeyboard(false);
}
function handleKeydown(event) {
	if (!isModifierKey(event)) setIsKeyboard(true);
}
function addListeners(currentDocument) {
	const abortController = new AbortController();
	currentDocument.addEventListener("mousedown", handleMousedown, { signal: abortController.signal });
	currentDocument.addEventListener("keydown", handleKeydown, { signal: abortController.signal });
	return abortController;
}
function useFocusVisible(componentRef) {
	(0, import_react.useEffect)(() => {
		var _a, _b;
		const currentDocument = (_b = (_a = componentRef === null || componentRef === void 0 ? void 0 : componentRef.current) === null || _a === void 0 ? void 0 : _a.ownerDocument) !== null && _b !== void 0 ? _b : document;
		let frame = frames.get(currentDocument);
		if (frame) frame.componentsCount++;
		else {
			frame = {
				componentsCount: 1,
				abortController: addListeners(currentDocument)
			};
			frames.set(currentDocument, frame);
		}
		return () => {
			frame.componentsCount--;
			if (frame.componentsCount === 0) {
				frame.abortController.abort();
				frames.delete(currentDocument);
			}
		};
	}, [componentRef]);
}
//#endregion
//#region node_modules/@cloudscape-design/components/internal/environment.js
var PACKAGE_SOURCE = "components";
var PACKAGE_VERSION = "3.0.0 (1b4a11ae)";
var GIT_SHA = "1b4a11ae";
var THEME = "open-source-visual-refresh";
var SYSTEM = "core";
//#endregion
//#region node_modules/@cloudscape-design/components/internal/base-component/index.js
initAwsUiVersions(PACKAGE_SOURCE, PACKAGE_VERSION);
function getBaseProps(props) {
	const baseProps = {};
	Object.keys(props).forEach((prop) => {
		if (prop === "id" || prop === "className" || prop.match(/^data-/)) baseProps[prop] = props[prop];
	});
	return baseProps;
}
/**
* Helper function to merge beta analytics metadata with the public analytics metadata api.
* Beta analytics metadata will override the public values to allow for safe migration.
*/
function getAnalyticsMetadataProps(props) {
	return {
		...props === null || props === void 0 ? void 0 : props.analyticsMetadata,
		...props === null || props === void 0 ? void 0 : props.__analyticsMetadata
	};
}
//#endregion
//#region node_modules/@cloudscape-design/components/internal/utils/get-visual-theme.js
var getVisualTheme = (theme, isVR) => {
	if (theme === "polaris" && isVR) return "vr";
	return theme;
};
//#endregion
//#region node_modules/@cloudscape-design/components/internal/hooks/use-visual-mode/index.js
var useVisualRefresh = () => true;
var useOneTheme = () => isThemeActive(Theme.OneTheme);
//#endregion
//#region node_modules/@cloudscape-design/components/internal/metrics.js
var metrics = new Metrics({
	packageSource: PACKAGE_SOURCE,
	packageVersion: PACKAGE_VERSION,
	theme: THEME
});
//#endregion
//#region node_modules/@cloudscape-design/components/internal/hooks/use-base-component/styles-check.js
function checkMissingStyles(ownerDocument) {
	if (!ownerDocument.defaultView) return;
	if (!getComputedStyle(ownerDocument.body).getPropertyValue(`--awsui-version-info-1b4a11ae`)) {
		console.error(`Missing AWS-UI CSS for theme "${THEME}", version "${PACKAGE_VERSION}", and git sha "${GIT_SHA}".`);
		metrics.sendOpsMetricObject("awsui-missing-css-asset", {});
	}
}
function documentReady(document, callback) {
	var _a;
	if (document.readyState === "complete") callback();
	else (_a = document.defaultView) === null || _a === void 0 || _a.addEventListener("load", () => callback(), { once: true });
}
async function documentReadyAndIdle(document, signal) {
	await new Promise((resolve, reject) => {
		signal.addEventListener("abort", () => reject(new DOMException("Aborted", "AbortError")));
		documentReady(document, () => {
			setTimeout(() => requestIdleCallback(() => resolve()), 1e3);
		});
	});
	const stylesheets = Array.from(document.querySelectorAll("link[rel=\"stylesheet\"]"));
	await Promise.all(stylesheets.map((link) => {
		if (link.sheet) return Promise.resolve();
		return new Promise((resolve, reject) => {
			function cleanup(done) {
				link.removeEventListener("load", onLoad);
				link.removeEventListener("error", onError);
				signal.removeEventListener("abort", onAbort);
				done();
			}
			const onLoad = () => cleanup(resolve);
			const onError = () => cleanup(resolve);
			const onAbort = () => cleanup(() => reject(new DOMException("Aborted", "AbortError")));
			link.addEventListener("load", onLoad);
			link.addEventListener("error", onError);
			signal.addEventListener("abort", onAbort);
		});
	}));
}
var checkedDocs = /* @__PURE__ */ new WeakMap();
var checkMissingStylesOnce = (document) => {
	if (!checkedDocs.get(document)) {
		checkMissingStyles(document);
		checkedDocs.set(document, true);
	}
};
function useMissingStylesCheck(elementRef) {
	(0, import_react.useEffect)(() => {
		var _a, _b;
		if (typeof requestIdleCallback !== "function") return;
		const ownerDocument = (_b = (_a = elementRef.current) === null || _a === void 0 ? void 0 : _a.ownerDocument) !== null && _b !== void 0 ? _b : document;
		const abortController = new AbortController();
		documentReadyAndIdle(ownerDocument, abortController.signal).then(() => checkMissingStylesOnce(ownerDocument), (error) => {
			// istanbul ignore next
			if (error.name !== "AbortError") throw error;
		});
		return () => abortController.abort();
	}, [elementRef]);
}
//#endregion
//#region node_modules/@cloudscape-design/components/internal/hooks/use-base-component/index.js
/**
* This hook is used for components which are exported to customers. The returned __internalRootRef needs to be
* attached to the (internal) component's root DOM node. The hook takes care of attaching the metadata to this
* root DOM node and emits the telemetry for this component.
*/
function useBaseComponent(componentName, config, analyticsMetadata) {
	const theme = getVisualTheme(THEME, useVisualRefresh());
	useComponentMetrics(componentName, {
		packageSource: PACKAGE_SOURCE,
		packageVersion: PACKAGE_VERSION,
		theme
	}, config);
	const elementRef = useComponentMetadata(componentName, {
		packageName: PACKAGE_SOURCE,
		version: PACKAGE_VERSION,
		theme
	}, analyticsMetadata);
	useMissingStylesCheck(elementRef);
	useFocusVisible(elementRef);
	return { __internalRootRef: elementRef };
}
//#endregion
//#region node_modules/@cloudscape-design/components/internal/utils/apply-display-name.js
function applyDisplayName(component, displayName) {
	component.displayName = displayName;
}
//#endregion
//#region node_modules/clsx/dist/clsx.m.js
function r(e) {
	var t, f, n = "";
	if ("string" == typeof e || "number" == typeof e) n += e;
	else if ("object" == typeof e) if (Array.isArray(e)) for (t = 0; t < e.length; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
	else for (t in e) e[t] && (n && (n += " "), n += t);
	return n;
}
function clsx() {
	for (var e, t, f = 0, n = ""; f < arguments.length;) (e = arguments[f++]) && (t = r(e)) && (n && (n += " "), n += t);
	return n;
}
//#endregion
export { useComponentMetadata as A, isHTMLElement as C, useStableCallback as D, createSingletonState as E, initAwsUiVersions as O, findUpUntil as S, createSingletonHandler as T, useDensityMode as _, useOneTheme as a, getGlobalFlag as b, getBaseProps as c, THEME as d, isModifierKey as f, useCurrentMode as g, isThemeActive as h, metrics as i, useComponentMetrics as k, PACKAGE_VERSION as l, isMotionDisabled as m, applyDisplayName as n, useVisualRefresh as o, Theme as p, useBaseComponent as r, getAnalyticsMetadataProps as s, clsx as t, SYSTEM as u, useReducedMotion as v, isNode as w, warnOnce as x, useRuntimeVisualRefresh as y };

//# sourceMappingURL=clsx.m-D3moizge.js.map